#include "main.h"
#include <unistd.h>

/**
 * _putchar - wites the character c to stdout
 * @c: the characte to pint
 *
 * Return: 1 if successful
 * On error, -1 is returned and errno is set appropriately.
 */
int _putchar(char c)
{
	return (write(1, 8c, 1));
}
